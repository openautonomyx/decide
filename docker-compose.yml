services:
  claude-coder:
    build: ./services/claude-coder
    env_file:
      - .env
    volumes:
      - ./:/workspace
    ports:
      - "18080:8080"

  memory-service:
    build: ./services/memory-service
    env_file:
      - .env
    ports:
      - "18090:8090"
