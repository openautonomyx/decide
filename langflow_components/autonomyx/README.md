# Autonomyx AgentNxt Bundle

## Included
- ProcessDocFlowBuilder (process + policy upload/parse/fill)
- SchemaOrgActionCatalog (schema.org actions as components)
- OrgSSOSettings (org SSO config for AgentNxt settings)

## Auth/SSO policy
All org auth is configured in AgentNxt settings (not backend env).

## Providers
- google
- github
- openai
- claude
- oidc (generic)
